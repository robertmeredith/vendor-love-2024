import { SectionHeading } from '@/components'
import { Preferences } from '@/components'

function UserSettings() {
  return (
    <>
      <SectionHeading heading="Settings" text="Manage your account settings" />
      <Preferences />
    </>
  )
}
export default UserSettings
