{
  "name": "vendor-details-2024",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"cd ./server && npm run dev\" \"cd ./client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
